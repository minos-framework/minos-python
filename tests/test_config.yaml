service:
    name: Order
rest:
    host: localhost
    port: 8900
    endpoints:
        - name: AddOrder
          route: /order
          method: POST
          controller: minos.services.OrderService
          action: add_order
events:
    broker: kafka
    port: 9092
    items:
        - name: TicketAdded
          controller: tests.services.CqrsTestService.CqrsService
          action: ticket_added
        - name: TicketDeleted
          controller: tests.services.CqrsTestService.CqrsService
          action: ticket_deleted
    queue:
        database: broker_db
        user: broker
        password: br0k3r
        host: postgres
        port: 5432
        records: 10
commands:
    broker: kafka
    port: 9092
    items:
        - name: AddOrder
          controller: minos.services.OrderService
          action: add_order
        - name: DeleteOrder
          controller: minos.services.OrderService
          action: delete_order
        - name: UpdateOrder
          controller: minos.services.OrderService
          action: update_order
        - name: GetOrder
          controller: minos.service.OrderService
          action: get_order
    queue:
        database: broker_db
        user: broker
        password: br0k3r
        host: postgres
        port: 5432
        records: 10
