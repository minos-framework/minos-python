service:
    name: Order
saga:
    storage:
        path: "./order.lmdb"
    items:
        - name: AddOrder
          controller: tests.order.SagaService
          action: ADD_ORDER
        - name: DeleteOrder
          controller: tests.order.SagaService
          action: DELETE_ORDER
    queue:
        database: order_db
        user: minos
        password: min0s
        host: localhost
        port: 5432
        records: 10
        retry: 2
