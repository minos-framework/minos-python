version: 2
name: order
databases:
  default:
    client: minos.plugins.sqlalchemy.SqlAlchemyDatabaseClient
    driver: postgresql+asyncpg
    database: order_db
    user: minos
    password: min0s
    host: localhost
    port: 5432
pools:
  database: minos.common.DatabaseClientPool
aggregate:
  entities:
    - minos.aggregate.testing.SnapshotRepositoryTestCase.NumbersList
    - minos.aggregate.testing.SnapshotRepositoryTestCase.Number
    - minos.aggregate.testing.SnapshotRepositoryTestCase.Owner
    - minos.aggregate.testing.SnapshotRepositoryTestCase.Car